public enum StorageConfiguration {
    // These identifiers and keys are compatibility contracts shared with
    // released builds and the Widget extension. Changes require migration and,
    // for the app group, matching entitlement updates.
    public static let appGroupIdentifier = "group.net.root-lump.date-plus"
    public static let pinnedDaysKey = "pinnedDays"
    public static let complicationDaysKey = "dayInfos"
    public static let daysToAddKey = "daysToAdd"
    public static let includeFirstDayKey = "includeFirstDay"
}
